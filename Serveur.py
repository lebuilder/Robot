#!/usr/bin/python
from mrpiZ_lib import *
import time

# tourner a droite
turnRight_degree(10, 180)

# tourner a gauche
turnLeft_degree(10, 90)


forward_mm(30, 500)
forward_mm(30, 500)
forward_mm(30, 500)
forward_mm(30, 500)
forward_mm(30, 500)
